<%- | 
  String[1] $domain,
  String[1] $wordlist_url,
| -%>
const serverEnv = {
  LOGIN_URL: 'http://<%= $domain %>/login?next={FROM_URL}',
  LOGOUT_URL: 'http://<%= $domain %>/logout?next={FROM_URL}',
  SESSION_URL: 'http://<%= $domain %>/userinfo',
  TOKEN_URL: 'http://<%= $domain %>/usertoken',
  ENDPOINTS: {
    wordlist : '<%= $wordlist_url %>'
  }
}

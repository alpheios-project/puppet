<%- | 
  String[1] $domain,
  String[1] $wordlist_url,
| -%>
const serverEnv = {
  SESSION_URL: 'http://<%= $domain %>/userinfo',
  TOKEN_URL: 'http://<%= $domain %>/usertoken',
  ENDPOINTS: {
    wordlist : '<%= $wordlist_url %>'
  }
}
